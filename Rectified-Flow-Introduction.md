# Rectified Flow：让我们一起流动 🚀

## 概述

本文简要介绍 Rectified Flow，内容基于讲义第一章。更多介绍请参考原始论文和相关博客。

## 问题：学习流生成模型

生成建模可以被表述为寻找一个计算过程，将噪声分布（记为 $\pi_0$）转换为从数据中观察到的未知数据分布 $\pi_1$。在流模型中，这个过程由常微分方程（ODE）表示：

$$\dot{Z}_t = v_t(Z_t), \quad \forall t \in [0,1], \quad \text{起始于 } Z_0 \sim \pi_0 \tag{1}$$

其中 $\dot{Z}_t = \mathrm dZ_t / \mathrm dt$ 表示时间导数，速度场 $v_t(x) = v(x, t)$ 是待学习的函数，需要确保当 $Z_0 \sim \pi_0$ 时，$Z_1$ 遵循目标分布 $\pi_1$。在这种情况下，我们说随机过程 $Z = \{Z_t\}$ 提供了从 $\pi_0$ 到 $\pi_1$ 的（ODE）传输。

需要注意的是，在所有非平凡情况下，只要存在一个这样的过程，就存在**无限多个**从 $\pi_0$ 到 $\pi_1$ 的 ODE 传输。因此，明确我们应该偏好哪种类型的 ODE 是很重要的。

一个选择是偏好在推理时**容易**求解的 ODE。在实践中，ODE 通过数值方法近似求解，通常构造 ODE 轨迹的**分段线性**近似。例如，一个常见的选择是欧拉方法：

$$\hat{Z}_{t+\epsilon} = \hat{Z}_t + \epsilon v_t(\hat{Z}_t), \quad \forall t \in \{0, \epsilon, 2\epsilon, \dots, 1\} \tag{2}$$

其中 $\epsilon > 0$ 是步长。改变步长 $\epsilon$ 会在精度和计算成本之间引入权衡：更小的 $\epsilon$ 产生更高的精度但需要更多的计算步骤。因此，我们应该寻找即使在大步长下也能准确近似的 ODE。

[图1：Lady Windermere's fan 图示欧拉方法轨迹误差累积，从不同初始点开始并随时间偏离真实解曲线]

理想情况出现在 ODE 遵循直线轨迹时，此时欧拉近似产生**零离散化误差**，无论步长如何选择。在这种情况下，ODE（在时间重参数化下）应满足：

$$Z_t = t Z_1 + (1 - t) Z_0, \quad \implies \quad \dot{Z}_t = Z_1 - Z_0.$$

这些 ODE，称为**直线传输**，能够实现可以在单步模拟的**快速**生成模型。我们将由此产生的 $(Z_0, Z_1)$ 对称为 $\pi_0$ 和 $\pi_1$ 的直线耦合。在实践中，我们可能无法达到完美的直线性，但我们可以努力使 ODE 轨迹尽可能直，以最大化计算效率。

## Rectified Flow

为了构造从 $\pi_0$ 到 $\pi_1$ 的流，假设我们有一个 $\pi_0$ 和 $\pi_1$ 的任意耦合 $(X_0, X_1)$，从中我们可以获得经验样本。这可以简单地是常见实践中的**独立耦合**，其分布为 $\pi_0 \times \pi_1$，当我们能访问 $\pi_0$ 和 $\pi_1$ 的独立样本时。其思想是取 $(X_0, X_1)$ 并将其转换为由 ODE 模型生成的更好的耦合。然后，我们可以选择性地重复这个过程来进一步增强所需的性质，如直线性。

Rectified Flow 通过以下方式构造：

### 构建插值

第一步是构建一个插值过程 $\{X_t\} = \{X_t : t \in [0, 1]\}$，在 $X_0$ 和 $X_1$ 之间平滑插值。虽然可以有一般性的选择，但让我们考虑标准的直线插值：

$$X_t = t X_1 + (1 - t) X_0.$$

这里的插值 $\{X_t\}$ 是以**"锚定-桥接"**方式生成的随机过程：我们首先采样端点 $X_0$ 和 $X_1$，然后采样连接它们的中间轨迹。

### 边际匹配

通过构造，插值过程 $\{X_t\}$ 的 $X_0$ 和 $X_1$ 的边际分布通过匹配目标分布 $\pi_0$ 和 $\pi_1$。然而，$\{X_t\}$ 不是像 $\dot{Z}_t = v_t(Z_t)$ 这样的**因果** ODE 过程，后者通过从 $Z_0$ 随时间前进生成输出 $Z_1$。相反，生成 $X_t$ 需要同时知道 $X_0$ 和 $X_1$，而不是仅仅从 $X_0$ 随 $t$ 增加而演化。

### 速度场估计

为了将插值过程转换为 ODE 流，我们需要估计一个速度场 $v_t$，使得当 ODE $\dot{Z}_t = v_t(Z_t)$ 从 $Z_0 \sim \pi_0$ 开始时，它能够近似重现插值过程的边际分布。一个自然的方法是通过最小化以下目标来学习速度场：

$$\min_{v_t} \mathbb{E}\left[\int_0^1 \|v_t(X_t) - \dot{X}_t\|^2 dt\right] \tag{3}$$

其中期望是关于插值过程 $\{X_t\}$ 的。这个目标函数鼓励 ODE 流近似跟随插值轨迹。

### 校正步骤

一旦我们有了速度场估计，就可以定义一个新的 ODE 流 $\{Z_t\}$：

$$\dot{Z}_t = v_t(Z_t), \quad Z_0 \sim \pi_0 \tag{4}$$

这个流将产生一个新的耦合 $(Z_0, Z_1)$。如果速度场估计准确，这个耦合应该比原始耦合 $(X_0, X_1)$ 具有更好的性质。特别是，如果我们使用直线插值，那么 ODE 流将倾向于产生更接近直线的轨迹。

### 迭代改进

校正步骤可以迭代应用：我们可以将新的耦合 $(Z_0, Z_1)$ 视为起点，重复上述过程来构建新的插值和 ODE 流。这种迭代可以进一步增强所需的性质。

## 实现细节

### 离散时间近似

在实践中，我们需要处理离散时间设置。给定时间步长 $\Delta t$，我们可以：

1. 在离散时间点采样插值过程
2. 使用有限差分近似速度
3. 应用数值积分方法求解 ODE

### 神经网络参数化

速度场通常使用神经网络参数化：

```python
class VelocityNet(nn.Module):
    def __init__(self, dim):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(dim + 1, 512),  # +1 for time
            nn.ReLU(),
            nn.Linear(512, 512),
            nn.ReLU(),
            nn.Linear(512, dim)
        )
    
    def forward(self, x, t):
        # x: 形状为 (batch_size, dim) 的张量
        # t: 形状为 (batch_size, 1) 的张量
        return self.net(torch.cat([x, t], dim=1))
```

### 训练算法

基本训练循环如下：

```python
def train_step(model, x0, x1, optimizer):
    # 采样时间点
    t = torch.rand(x0.shape[0], 1)
    
    # 计算插值点
    xt = t * x1 + (1-t) * x0
    
    # 计算目标速度
    v_target = x1 - x0  # 直线情况下的速度
    
    # 预测速度
    v_pred = model(xt, t)
    
    # 计算损失
    loss = F.mse_loss(v_pred, v_target)
    
    # 优化
    optimizer.zero_grad()
    loss.backward()
    optimizer.step()
    
    return loss.item()
```

## 优势与应用

### 主要优势

1. **快速采样**：直线轨迹允许使用较大的步长
2. **稳定性**：减少数值误差累积
3. **质量保证**：理论上保证边际分布匹配

### 应用场景

1. **图像生成**：
   - 高分辨率图像合成
   - 图像编辑和操作

2. **3D 生成**：
   - 点云生成
   - 3D 形状建模

3. **序列生成**：
   - 音频合成
   - 视频生成

## 未来方向

1. **理论扩展**：
   - 更一般的插值方案
   - 最优传输理论的联系

2. **算法改进**：
   - 自适应步长选择
   - 混合采样策略

3. **应用拓展**：
   - 条件生成
   - 跨模态生成

## 参考资料

1. [原始论文](https://arxiv.org/abs/2209.14577)
2. [项目主页](https://rectifiedflow.github.io/)
3. [代码实现](https://github.com/gnobitab/RectifiedFlow)

## 附录：完整代码示例

```python
# 完整的训练和采样实现
# 详见 GitHub 仓库
``` 